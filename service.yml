apiVersion: v1
kind: Service
metadata:
  name: pcc_auth
  labels:
    app: pcc_auth
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8081
  selector:
    app: pcc_auth
  sessionAffinity: None
